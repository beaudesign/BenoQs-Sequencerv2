## 2026-09-09

**Intend to change:** Refresh `journal/STATE.md` after the Metronome phrase/
rotate pass (it still listed phrase playback and Grid-mutation as unbuilt),
and expose step GRV (`Phrase` / `PhrasePos`) on the octoffi Grid-mutation
surface now that those fields actually drive playback.
**How I'll know it worked:** STATE's octocore / octoffi / fan-out sections
match `main`+this branch; `cargo test -p octoffi` still green.

### What I did

- Rewrote the stale octocore / octoffi / fan-out blocks in STATE.md.
- Added `OctoStepAttr::Phrase` / `PhrasePos` to octoffi + `octoffi.h`.

Journal: journal/conductor/2026-09-09.md

---

## 2026-09-09 (later)

**Intend to change:** Refresh STATE.md after the factory-phrase / POS pass.
**How I'll know it worked:** fan-out no longer lists those two as unbuilt.

STATE's octocore / fan-out blocks updated. Test count 56 → 66.

Journal: journal/conductor/2026-09-09.md

---

## 2026-09-09 (map-factor pass)

**Intend to change:** refresh STATE after map-factor events, and expose the
new VEL/PIT/AMT/GRV/MCC map-factor fields on the octoffi track surface.
**How I'll know it worked:** fan-out no longer lists map-factor events as
unbuilt; `cargo test -p octoffi` still green.

STATE octocore / octoffi / fan-out updated. Test count 66 → 81 unit, 3 → 4
fixtures. Map-factor get/set appended to `OctoTrackAttr` (existing
discriminants unchanged).

Journal: journal/conductor/2026-09-09.md
